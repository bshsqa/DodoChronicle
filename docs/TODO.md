# TODO

## 범례
- [ ] 미구현 / 진행 예정

---

## 설정 & 관리

---

## 타임라인 & 사진 UX

- [ ] **#12** 날짜카드에서 사진 추가 기능 제공 (이슈 #19)
  > 현재는 사진 삭제만 가능하므로, 날짜카드에서 바로 사진을 추가할 수 있는 진입점(FAB/버튼/메뉴)을 제공
- [ ] **#13** 텍스트 이벤트(한 일/한 말/기타) 숨김 기능 추가 + 숨김 목록 관리 (이슈 #19)
  > 삭제 대신 숨김 처리(복구 가능) 지원. 설정 옵션에 "숨김 아이템" 메뉴를 추가하고, 숨김 목록에서 롱프레스로 개별 복원
- [ ] **#14** 날짜카드 "사진+" 액션으로 그날 전체 사진 보기 (이슈 #19)
  > 모든 날짜카드에 공통으로 "사진+" 버튼을 노출. 기본 카드는 기존처럼 유지하고 버튼을 누르면 새 창에서 해당 날짜의 사진을 리스트업해 탐색 가능

---

## 검색 UX

- [ ] **#15** 타임라인 상단 검색창 추가 (즐겨찾기 필터와 동일한 화면 흐름)
  > 검색 결과를 별도 페이지로 분리하지 않고, 기존 타임라인 화면에서 조건에 맞는 날짜카드만 필터링해 표시
- [ ] **#16** 검색 결과 정렬은 가중치 없이 날짜순 유지
  > 키워드/문맥 검색 여부와 관계없이 결과 정렬은 기존 타임라인 정책(날짜순)과 동일하게 적용
- [ ] **#17** 검색 모드는 통합 기본 + 선택 옵션 제공
  > 기본은 키워드+문맥 통합 검색으로 제공하고, 필요 시 옵션에서 키워드만/문맥 포함을 선택 가능하게 구성

---

## AI / LLM 품질

- [ ] **#6** 새로 로딩된 날짜의 콘텐츠를 LLM에게 전달해 특별한 날 감지 → 날짜카드 옆에 짧게 표시

- [ ] **#10** 프롬프트에 아이 이름 / 나이(청크 날짜 기준) / 성별 정보 전달 및 무관한 이벤트 필터링
  > `GeminiEventClassifier.buildPrompt()`에서 이름·생년월일·청크 기준 나이·성별 모두 포함됨. 아이를 가리키는 다양한 표현(애기, 왕자님 등) 인식 지침도 포함.

---

## 데이터 & 표시

- [ ] **#8** 한 일 / 한 말 / 기타 이벤트에 출처 대화방 별명 표시
  > EventEntity에 roomId/roomName 없음 → DB 마이그레이션 + UseCase + UI 전 레이어 수정 필요
