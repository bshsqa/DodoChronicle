# TODO

## 범례
- [x] 완료
- [ ] 미구현 / 진행 예정

---

## 설정 & 관리

- [ ] **#1** 설정 아이콘이 바로 초기화 다이얼로그를 여는 대신, 선택 메뉴를 띄우고 초기화를 선택지 중 하나로 배치
- [ ] **#2** 설정 메뉴에 로그보기 항목 추가 (Gemini API 호출 결과, 실패 이력 등)
- [ ] **#3** 설정 메뉴에 신규 사진 수동 로딩 항목 추가 (6시간 자동 주기 외 수동 트리거)
  > ※ #1 선행 작업 완료 후 #2, #3과 묶어서 구현

---

## 타임라인 & 사진 UX

- [ ] **#4** 날짜카드에서 사진 탭 시 전체화면으로 확대 + 같은 날 사진 간 슬라이드 이동
- [ ] **#5** 백그라운드 작업(사진 스캔 / 카카오 import) 완료 시 시스템 알림 표시
  > 사진 스캔: `ScanForegroundService.showCompletedNotification()` · 카카오 import: `KakaoImportService.showCompletedNotification()` + 인앱 `ImportDoneOverlay` 모두 구현됨
- [x] **#11** 날짜카드 헤더에 포함된 콘텐츠 유형 뱃지 표시 (사진 / 한 일 / 한 말 / 기타)
  > → **ENH-006** 참고

---

## AI / LLM 품질

- [ ] **#6** 새로 로딩된 날짜의 콘텐츠를 LLM에게 전달해 특별한 날 감지 → 날짜카드 옆에 짧게 표시
- [x] **#7** 실패한 청크 재시도 기능 (완료 화면에서 재시도 버튼으로 해당 청크만 재처리)
  > 현재: 실패 청크 수만 표시되고 데이터 유실 → **ENH-006** 참고

  **실패 원인**:
  - Gemini API rate limit (429) — 12초 delay 있으나 간헐적 발생
  - API 응답 파싱 실패 — JSON 형식 불일치
  - 네트워크 에러 — 일시적 연결 끊김

- [ ] **#10** 프롬프트에 아이 이름 / 나이(청크 날짜 기준) / 성별 정보 전달 및 무관한 이벤트 필터링
  > `GeminiEventClassifier.buildPrompt()`에서 이름·생년월일·청크 기준 나이·성별 모두 포함됨. 아이를 가리키는 다양한 표현(애기, 왕자님 등) 인식 지침도 포함.
- [x] **#9** rawExcerpt 길이 상향 (3~7줄 → 5~10줄) + 무관한 내용 배제 지침 추가
  > → **ENH-006** 참고

---

## 데이터 & 표시

- [ ] **#8** 한 일 / 한 말 / 기타 이벤트에 출처 대화방 별명 표시
  > EventEntity에 roomId/roomName 없음 → DB 마이그레이션 + UseCase + UI 전 레이어 수정 필요
