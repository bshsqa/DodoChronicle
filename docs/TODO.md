# TODO

## 진행 원칙
- 열린 GitHub 이슈 기준으로 우선순위를 관리한다.
- 완료되었거나 대체 구현으로 해결된 이슈는 GitHub에서 정리 후 닫는다.
- 배포 전 필수 작업과 장기 개선 작업을 구분한다.

---

## 우선순위 작업 목록

- [ ] **#29 사진 카테고라이징 정확도 개선**
  > 추출하는 사진 수는 유지하되, 얼굴/사진 카테고라이징 판정을 더 보수적으로 조정한다. threshold, 확인 UI, 제외 정책을 함께 점검한다.

- [ ] **#30 Gemini API 키 입력**
  > 옵션에서 Gemini API 키를 입력/저장/검증할 수 있게 한다. API 키가 없으면 카카오톡 임포트와 문맥 업데이트를 실행하지 않도록 막는다. 배포 전 필수 작업이다.

- [ ] **#32 앱 아이콘 만들기**
  > 배포 전 polish 작업. DodoChronicle 분위기에 맞는 Android adaptive icon을 제작한다.

- [ ] **#31 텍스트 이벤트 DB export/import**
  > 텍스트 이벤트 데이터를 export하고, 다운로드 가능한 링크 또는 파일을 통해 import할 수 있게 한다. 스키마 버전, 개인정보, 검색 보조 인덱스 포함 여부를 별도 설계한다.

- [ ] **#34 문맥 검색 결과 업그레이드**
  > 현재 문맥 검색은 실사용 가능한 수준으로 정리되었으나, 검색 결과 품질 개선 여지가 남아 있다. 토큰 정제, 관련도 점수, 결과 설명/디버그, 날짜순과 관련도순 체감 차이를 후속 개선 항목으로 관리한다.

---

## 참고
- #19, #25는 열린 이슈 목록에서 제외되어 완료/종료된 것으로 본다.
- #28은 ENH-012/ENH-013 및 관련도 정렬 수정으로 해결되어 GitHub에서 완료 처리했다.
- #26, #27, #33은 ENH-014 구현으로 해결되어 GitHub에서 완료 처리했다.
- #35와 원본 삭제된 사진 처리는 ENH-015 구현으로 해결되어 GitHub/TODO에서 완료 처리했다.
