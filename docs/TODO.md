# TODO

## 진행 원칙
- 열린 GitHub 이슈 기준으로 우선순위를 관리한다.
- 완료되었거나 대체 구현으로 해결된 이슈는 GitHub에서 정리 후 닫는다.
- 배포 전 필수 작업과 장기 개선 작업을 구분한다.

---

## 우선순위 작업 목록

- [ ] **#33 신규 사진 로딩 눌렀을 때 동작 정리**
  > 신규 사진 로딩은 마지막 처리 시점 이후 사진만 대상으로 해야 한다. 중복으로 다시 50개를 스캔하지 않도록 하고, 수동 로딩 시 로딩 UI와 완료 후 확인/카테고리 선택 흐름이 자연스럽게 이어지도록 정리한다.

- [ ] **#27 오버레이/상단 UI 표시 중 하위 터치 차단**
  > 카카오톡 임포트, 로딩 오버레이 등 상단 또는 전체 화면 UI가 떠 있을 때 아래 아이콘/버튼이 눌리지 않도록 터치 소비를 보장한다.

- [ ] **#26 상단 아이콘 순서와 진입 경로 정리**
  > 설정은 가장 오른쪽으로 이동하고, 카카오톡 임포트는 옵션 안으로 넣는 방향을 검토한다. 상단 앱바는 자주 쓰는 핵심 액션만 남겨 복잡도를 낮춘다.

- [ ] **#28 검색기능 정리 후 close**
  > ONNX 임베딩 기반 검색은 제거했고, Gemini 검색 보조 인덱스 기반 문맥 검색으로 대체했다. ENH-012/ENH-013 구현 내용을 기준으로 실제 동작 확인 후 GitHub 이슈에 정리 코멘트를 남기고 닫는다.

- [ ] **#29 사진 카테고라이징 정확도 개선**
  > 추출하는 사진 수는 유지하되, 얼굴/사진 카테고라이징 판정을 더 보수적으로 조정한다. threshold, 확인 UI, 제외 정책을 함께 점검한다.

- [ ] **#30 Gemini API 키 입력**
  > 옵션에서 Gemini API 키를 입력/저장/검증할 수 있게 한다. API 키가 없으면 카카오톡 임포트와 문맥 업데이트를 실행하지 않도록 막는다. 배포 전 필수 작업이다.

- [ ] **#32 앱 아이콘 만들기**
  > 배포 전 polish 작업. DodoChronicle 분위기에 맞는 Android adaptive icon을 제작한다.

- [ ] **#31 텍스트 이벤트 DB export/import**
  > 텍스트 이벤트 데이터를 export하고, 다운로드 가능한 링크 또는 파일을 통해 import할 수 있게 한다. 스키마 버전, 개인정보, 검색 보조 인덱스 포함 여부를 별도 설계한다.

---

## 참고
- #19, #25는 열린 이슈 목록에서 제외되어 완료/종료된 것으로 본다.
- #28은 이번 검색 구조 변경으로 해결된 항목이므로 별도 확인 후 닫는 후보이다.
